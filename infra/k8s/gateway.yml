apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gittex-gateway
spec:
  gatewayClassName: envoy
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: All